---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: open-webui-pg-app-push
spec:
  deletionPolicy: None
  refreshInterval: 24h
  secretStoreRefs:
    - name: 1password-sdk
      kind: ClusterSecretStore
  selector:
    secret:
      name: open-webui-pg-app
  data:
    - match:
        secretKey: username
        remoteRef:
          remoteKey: open-webui-pg-app
          property: username
    - match:
        secretKey: password
        remoteRef:
          remoteKey: open-webui-pg-app
          property: password
    - match:
        secretKey: uri
        remoteRef:
          remoteKey: open-webui-pg-app
          property: uri
    - match:
        secretKey: uri-pooler-rw
        remoteRef:
          remoteKey: open-webui-pg-app
          property: uri-pooler-rw
    - match:
        secretKey: uri-pooler-ro
        remoteRef:
          remoteKey: open-webui-pg-app
          property: uri-pooler-ro
