apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: minio
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer
spec:
  gatewayClassName: cilium
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: iceberg.internal.vw-ops.net
  listeners:
  - protocol: HTTP
    hostname: iceberg.internal.vw-ops.net
    port: 80
    name: http
  - protocol: HTTPS
    hostname: iceberg.internal.vw-ops.net
    port: 443
    name: https
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: secret-tls-minio-local 