apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: 1password-sa-token
  namespace: flux-system
spec:
  refreshPolicy: OnChange
  secretStoreRef:
    name: 1password-sdk
    kind: ClusterSecretStore
  data:
  - secretKey: token
    remoteRef:
      key: external-secrets-sa/password
