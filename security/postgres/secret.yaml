apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nessie-pg
  namespace: security
spec:
  refreshPolicy: OnChange
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  data:
    - secretKey: username
      remoteRef:
        key: nessie-pg
        property: username
    - secretKey: password
      remoteRef:
        key: nessie-pg
        property: password

