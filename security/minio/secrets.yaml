apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: siem-data-user
  namespace: security
spec:
  refreshPolicy: OnChange
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  data:
    - secretKey: username
      remoteRef:
        key: siem-data-user
        property: username
    - secretKey: password
      remoteRef:
        key: siem-data-user
        property: password 
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: siem-data-root-user
  namespace: security
spec:
  refreshPolicy: OnChange
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  target:
    name: siem-data-root-user
    creationPolicy: Owner
  data:
    - secretKey: rootUser
      remoteRef:
        key: siem-data-root-user
        property: username
    - secretKey: rootPassword
      remoteRef:
        key: siem-data-root-user
        property: password 
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: siem-data-svc
  namespace: security
spec:
  refreshPolicy: OnChange
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password-sdk
  target:
    name: siem-data-svc
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: siem-data-svc
        property: username
    - secretKey: password
      remoteRef:
        key: siem-data-svc
        property: password 